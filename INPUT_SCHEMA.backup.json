{
  "title": "Directory Listing Extractor - Input",
  "description": "Configure directory/map extraction from embedded JS objects or DOM listings.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Mode",
      "description": "Extraction mode. Use embedded_js for JS-embedded listing objects. Use dom for HTML listing cards.",
      "type": "string",
      "default": "embedded_js",
      "editor": "select",
      "enum": ["embedded_js", "dom"]
    },
    "startUrls": {
      "title": "Start URLs",
      "description": "One or more directory/map pages to crawl.",
      "type": "array",
      "editor": "requestListSources",
      "default": [{ "url": "https://regenerationcanada.org/en/map/" }]
    },
    "maxListings": {
      "title": "Max listings",
      "description": "Safety cap for number of dataset items produced.",
      "type": "integer",
      "default": 500,
      "minimum": 1,
      "maximum": 50000
    },
    "maxPages": {
      "title": "Max pages",
      "description": "Safety cap for number of pages visited.",
      "type": "integer",
      "default": 1,
      "minimum": 1,
      "maximum": 1000
    },
    "debug": {
      "title": "Debug logging",
      "description": "Print verbose logs for troubleshooting (responses, taxonomy detection, counts).",
      "type": "boolean",
      "default": false
    },
    "outputCsv": {
      "title": "Export CSV",
      "description": "Generate output.csv in the run output (in addition to dataset).",
      "type": "boolean",
      "default": true
    },
    "outputXlsx": {
      "title": "Export XLSX",
      "description": "Generate output.xlsx in the run output (in addition to dataset).",
      "type": "boolean",
      "default": true
    },
    "outputColumnsMode": {
      "title": "Output columns mode",
      "description": "default = core columns only, all = includes extra fields (address, quote, logo, etc.).",
      "type": "string",
      "default": "default",
      "editor": "select",
      "enum": ["default", "all"]
    },
    "outputBaseName": {
  "title": "Output base name",
  "description": "Base name for exported files (without extension).",
  "type": "string",
  "default": "output",
  "editor": "textfield"
},

    "embedded": {
      "title": "Embedded JS settings",
      "description": "Settings used only in embedded_js mode to extract listing objects from the page HTML/JS.",
      "type": "object",
      "default": {
        "anchorKey": "logoMedium",
        "keys": [
          "name",
          "loc",
          "address",
          "lat",
          "lng",
          "email",
          "tel",
          "website",
          "pageURL",
          "categories",
          "products",
          "services",
          "buy",
          "size",
          "results",
          "quote",
          "logo",
          "logoMedium"
        ],
        "fieldMap": {
          "name": "entity_name",
          "loc": "location",
          "address": "address",
          "lat": "lat",
          "lng": "lng",
          "email": "email",
          "tel": "phone",
          "website": "website",
          "pageURL": "profile_url",
          "categories": "categories",
          "products": "products",
          "services": "services",
          "buy": "how_to_buy",
          "size": "size",
          "results": "results",
          "quote": "quote",
          "logo": "logo",
          "logoMedium": "logo_medium"
        }
      }
    },
    "taxonomy": {
      "title": "Taxonomy overrides",
      "description": "Optional manual decoding overrides for category/service codes (rpc*/rss*). Manual values override static and auto-detected maps.",
      "type": "object",
      "default": { "categoryMap": {}, "serviceMap": {} }
    }
  },
  "required": ["startUrls"],
  "additionalProperties": true
}
